# DexWatcher

DexWatcher is a small Python project for monitoring token activity and sending updates.

## Quick Start

1. Install dependencies:
   - `poetry install`
2. Run the app:
   - `python dex.py`

## Files

- `dex.py` – main script
- `config.json` – project config
- `utils/` – helper functions
